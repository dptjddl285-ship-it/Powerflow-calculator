# 📋 프로젝트 마스터 플랜 및 초기 기획서 (VisionFlow)

### Edge AI 기반 전력계통 단선도(SLD) 인식 및 조류계산 자동화 시스템

---

> [!IMPORTANT]
> **본 문서의 목적 (Purpose of this Document)**
> 
> 이 문서는 완성된 제품의 상세 스펙이 아닌, **'전력계통대회 프로젝트를 어떻게 시작하고 완주할 것인가'**를 정의하는 **첫 단계 기획서(Master Plan)**입니다.
> 팀의 최종 목표를 명확히 하고, 이를 달성하기 위해 어떤 기술(프로그램)을 사용하며, 팀원들이 어떤 교육과 학습을 병행해야 하는지에 대한 로드맵을 제시합니다.

---

## 📌 1. 프로젝트 최종 목표 (Project Goal)

| 항목 | 내용 |
| :--- | :--- |
| **최종 비전** | 수작업으로 진행되던 조류계산(Power Flow) 모델링을 AI 이미지 인식 기반으로 자동화/간소화하는 혁신적인 솔루션 개발 |
| **핵심 결과물** | 기존에 가지고 있는 **회로도 도면 사진(이미지)을 앱에 업로드**하면, AI가 자동으로 기기와 선로를 파악하고 조류계산까지 원스톱으로 처리하는 애플리케이션(App) |
| **기대 효과** | 기존 PSS/E 등 전문 소프트웨어에서 발생하던 막대한 수작업 모델링 시간(수백 개 기기 배치 및 선로 연결)을 획기적으로 단축 |

---

## 🛠 2. 필요 프로그램 및 기술 스택 (Tech Stack)

프로젝트를 성공적으로 구현하기 위해 팀에서 도입해야 할 핵심 프로그램과 기술은 다음과 같습니다.

### 2.1. 인공지능 & 영상 처리 (AI & Computer Vision)
* **YOLO11 (Ultralytics):** 도면 이미지에서 4대 핵심 심볼(Bus, Generator, Load, Transformer)의 위치를 빠르고 정확하게 찾아내는 객체 인식 AI 모델.
* **Roboflow:** AI가 학습할 도면 이미지의 바운딩 박스를 라벨링(Labeling)하고 관리하는 플랫폼.
* **OpenCV (Python):** AI가 심볼을 찾은 뒤, 남은 이미지에서 '전선(Wire)'이 어떻게 연결되어 있는지 추적(뼈대 추출, 이진화 등)하는 영상 처리 라이브러리.

### 2.2. 백엔드 & 전력계통 알고리즘 (Backend & Algorithm)
* **FastAPI (Python):** 앱에서 이미지를 보내면 AI 분석과 조류계산을 수행한 뒤 결과를 앱으로 다시 돌려주는 가볍고 빠른 서버 프레임워크.
* **pandapower (또는 Newton-Raphson 직접 구현):** 추출된 회로망 구조를 바탕으로 전압, 위상각, 유효/무효 전력을 계산하는 Python 기반 전력계통 해석 라이브러리.

### 2.3. 프론트엔드 앱 (Frontend App)
* **Flutter (Dart):** 하나의 코드로 안드로이드/iOS 앱을 동시에 만들 수 있는 크로스 플랫폼 프레임워크.
* **Canvas API (CustomPaint):** 분석된 회로도를 화면에 그리고, 사용자가 직접 터치하여 선을 긋거나 심볼을 수정할 수 있도록 돕는 인터랙티브 그래픽 도구.

---

## 🚀 3. 프로젝트 개발 진행 순서 (Development Roadmap)

이 프로젝트는 막연한 동시 다발적 개발이 아닌, 아래의 6단계 순서에 따라 차근차근(Step-by-Step) 진행되어야 합니다. 앞 단계가 완성되어야 다음 단계로 넘어갈 수 있습니다.

### [Step 1] 데이터 수집 및 라벨링 (Roboflow)
- **작업 내용:** IEEE 14, 30 Bus 등 다양한 전력계통 단선도 이미지(jpg/png)를 수집합니다.
- **사용 툴:** Roboflow 플랫폼
- **목표:** 수집한 이미지에 등장하는 핵심 기기(Bus, Generator, Load, Transformer)에 네모 박스를 치고(Bounding Box), AI가 학습할 수 있는 정답지 데이터셋을 구축합니다.

### [Step 2] AI 객체 인식 모델 훈련 (YOLO & Colab)
- **작업 내용:** Step 1에서 만든 데이터셋을 가져와 인공지능을 가르칩니다.
- **사용 툴:** Google Colab, YOLO11 (PyTorch)
- **목표:** 도면 사진을 넣었을 때 기기들을 정확도 90% 이상으로 찾아내는 AI 두뇌(가중치 파일, `best.pt`)를 완성합니다.

> [!WARNING]
> **🚨 핵심 주의사항: 괴물 박스(False Positive) 이슈 방지**
> - **문제점:** AI의 훈련 점수(mAP)가 높게 나오더라도, 실제 회로도를 넣었을 때 Bus(모선) 기호 등이 화면 전체를 뒤덮는 거대한 네모 상자(일명 '괴물 박스')로 잘못 인식되는 치명적인 오류가 잦습니다.
> - **사전 해결 방안 (필수 적용):**
>   1. **데이터 증강 제어:** Roboflow에서 데이터 증강(Augmentation) 시, **Rotation(회전)과 같은 공간 왜곡 증강을 절대 사용하지 마세요.** 선형 객체인 회로도가 왜곡되어 오인식을 유발합니다. (해상도 유지 및 mosaic 동적 증강 정도만 사용)
>   2. **NMS 필터링:** 실제 인식(Inference) 단계에서 NMS 파라미터(예: Confidence 0.5~0.6 이상, IoU 0.45 이하)를 꼼꼼히 세팅하여 비정상적으로 겹치거나 큰 박스를 쳐내야 합니다.
> - **다음 단계로 넘어가는 조건:** 단순히 `best.pt` 점수만 믿고 넘어가지 마세요. **반드시 처음 보는 실제 도면 이미지 여러 장을 모델에 넣어 괴물 박스가 단 하나도 나오지 않음을 두 눈으로 직접 검증(시각화 테스트)**한 뒤에만 Step 3로 넘어가야 합니다.

### [Step 3] 백엔드 및 회로 연결망(OpenCV) 추출 개발
- **작업 내용:** AI가 찾은 기기 위치 정보를 바탕으로 선(Wire)이 어떻게 연결되어 있는지 파악합니다.
- **사용 툴:** Python, OpenCV, FastAPI
- **목표:**
  1. AI가 찾은 기기 부분을 사진에서 지웁니다(마스킹).
  2. OpenCV를 이용해 남은 뼈대(전선)를 추적하여 '어느 기기와 어느 기기가 연결되었는지' 알아냅니다.
  3. 이 최종 회로 연결망을 앱으로 보낼 수 있도록 JSON 데이터 형태로 가공하는 백엔드 서버를 띄웁니다.

### [Step 4] 프론트엔드 앱 기초 뼈대 만들기 (Flutter)
- **작업 내용:** 사용자가 직접 만지는 모바일/웹 앱의 기초 껍데기를 만듭니다.
- **사용 툴:** Flutter
- **목표:** 앱에 도면 이미지 업로드 버튼을 만들고, 사진을 업로드하면 백엔드(Step 3) 서버로 전송한 뒤, 완성된 JSON 데이터를 성공적으로 받아오는지 통신 테스트를 완료합니다.

### [Step 5] 인터랙티브 회로 캔버스 및 수동 에디터 구현 (Human-in-the-Loop)
- **작업 내용:** 앱에서 받아온 데이터를 화면에 실제 회로도처럼 그려주고 편집하게 만듭니다.
- **사용 툴:** Flutter (CustomPaint / Canvas API)
- **목표:**
  1. JSON 데이터를 바탕으로 화면에 기기와 선을 예쁘게 그립니다.
  2. AI나 OpenCV가 완벽하지 않아 끊어진 선이 있다면, **사용자가 앱 화면에서 터치와 드래그로 직접 선을 긋거나 기기를 추가/삭제**할 수 있는 강력한 '수동 에디터' 기능을 완성합니다.

### [Step 6] 엑셀 매핑 및 조류계산 연동 (최종 통합)
- **작업 내용:** 그려진 회로도에 실제 전기적 데이터(저항, 전압 등)를 입히고 조류계산을 수행합니다.
- **사용 툴:** Flutter, Python (pandapower 또는 Newton-Raphson)
- **목표:** 
  1. 앱에 미리 준비된 엑셀 파일(각 기기의 상세 스펙)을 업로드하여 화면의 기기들과 매핑(연결)합니다.
  2. 앱에서 [조류계산 실행] 버튼을 누르면 서버가 계산을 수행합니다.
  3. 전압 크기, 위상각, 선로 조류 흐름 등의 최종 결과값을 앱 화면 캔버스 위에 실시간 그래픽(화살표, 숫자 뱃지 등)으로 오버레이하여 보여주면 프로젝트가 최종 완성됩니다.
