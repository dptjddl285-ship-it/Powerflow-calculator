# PowerLens 적응형 인식 파이프라인 설계

## 1. 목표와 한계

이 파이프라인의 목표는 모든 인터넷 단선도를 조건문만으로 100% 맞히는
것이 아니다. 지원하는 IEEE 계열 기호군에서 높은 자동 인식률을 유지하고,
인식 결과가 조류계산에 들어가기 전에 전기적 불가능 상태를 자동으로
차단하는 것이 목표다.

핵심 원칙은 다음과 같다.

1. 이미지 한 장에 맞춘 예외를 추가하지 않는다.
2. 모든 객체는 `후보 생성 → 필수조건 검증 → 사유별 구원 → 탈락/검토`를
   거친다.
3. 구원 단계도 필수 전기 연결 조건을 우회할 수 없다.
4. 객체 인식 점수가 높아도 그래프가 불가능하면 조류계산을 실행하지 않는다.
5. 기존 회귀 세트를 모두 통과한 규칙만 생산 코드에 남긴다.

## 2. 전체 흐름

```text
이미지 디코딩
  ↓
화질 프로파일링(해상도/선 두께/대비/흐림)
  ↓
두 레이어 생성
  ├─ 객체 레이어: 원본 또는 화질 정규화 영상
  └─ 선로 레이어: 숫자·문자 성분을 제거한 conductor mask
  ↓
버스 후보 생성 및 확정
  ↓
부하/발전기/변압기 후보 생성
  ↓
객체별 필수조건 검증
  ↓
사유가 명확한 후보만 한 번 구원
  ↓
객체 마스킹 + 포트 기반 선로 추적
  ↓
그래프 전역 검증
  ↓
국소 재추적 1회 또는 사용자 검토 요청
  ↓
전기 파라미터/Slack bus 검증
  ↓
조류계산
```

숫자 필터링은 원본 이미지에 직접 적용하지 않는다. 작은 화살표나 변압기
곡선도 숫자와 비슷한 크기이기 때문이다. 객체 인식은 원본 레이어에서
수행하고, 숫자 때문에 선로가 잘못 이어지는 문제는 별도의 선로 레이어에서
해결한다.

## 3. 이미지 프로파일과 해상도 분기

`backend_api/core/pipeline_policy.py::inspect_image`가 다음 값을 측정한다.

- 원본 폭과 높이
- Otsu 이진화 기반 전경 비율
- 5~95 백분위 명암 대비
- Laplacian 기반 흐림 정도
- distance transform 기반 대표 선 두께

### 정상 해상도

- 짧은 변이 720px 이상이고 선 두께가 안정적이면 원본을 그대로 사용한다.
- 현재 1280×1280 회귀 세트는 이 경로를 사용한다.

### 저해상도

- 짧은 변이 720px 미만이거나 대표 선 두께가 1.6px 미만이면 저해상도로
  분류한다.
- 짧은 변이 약 900px, 대표 선 두께 약 2.6px가 되도록 1.25~3배 확대한다.
- 좌표와 선로 path는 분석 후 원본 좌표로 되돌린다.
- 끊어진 선 허용 범위는 조금 늘리지만, 버스의 실제 branch와 부하의
  tail-to-bus 연결 조건은 완화하지 않는다.

### 고해상도

- 긴 변이 2800px를 넘으면 메모리와 형태학 커널 왜곡을 막기 위해 약
  2400px 수준으로 축소하고 결과 좌표를 복원한다.

### 저대비/흐림

- CLAHE는 객체 원본 전체가 아니라 conductor mask 생성에만 사용한다.
- 흐림이 심해 가는 선로가 복구되지 않으면 무리하게 연결하지 않고
  `needs_review`로 보낸다.

## 4. 후보 상태

모든 후보는 다음 네 상태 중 하나를 갖는다.

| 상태 | 의미 |
| --- | --- |
| `accept` | 필수 CV/전기 조건을 모두 통과 |
| `rescue` | 특정 실패 사유만 보정한 뒤 필수조건을 통과 |
| `review` | 객체 가능성은 있지만 topology 또는 의미 확인 필요 |
| `reject` | 필수조건 위반으로 자동 결과에서 제외 |

구원은 `fragmented_bus`, `outline_load`, `tiny_load`,
`local_transformer_pair`처럼 사유별로 분리한다. 단순히 confidence가 높다는
이유만으로 탈락 후보를 되살리지 않는다.

## 5. 객체별 규칙

### 5.1 Bus

필수조건:

- 수평 또는 수직의 단일 축을 가져야 한다.
- 종횡비, 직선성, 두께 프로파일이 해당 이미지의 bus family와 일치해야 한다.
- 최소 한 개의 선로/발전기/부하/변압기 branch가 들어오거나 나가야 한다.
- 끝점에서 꺾여 다른 방향으로 진행하면 선로로 보고 탈락시킨다.
- 숫자/문자 획이 후보 면적의 대부분이면 탈락시킨다.
- 화면을 덮는 monster box는 가장 먼저 제거한다.

저해상도 구원:

- 1픽셀 수준으로 끊긴 직선 막대만 morphology로 연결한다.
- CV 직선·profile·branch 조건을 다시 검사한다.
- 같은 위치에 YOLO bus 제안이 있을 때만 최종 구원한다.
- endpoint bend 또는 branch 없음은 절대 구원하지 않는다.

### 5.2 Load

필수조건:

- 삼각형/화살표/외곽선 화살표의 compact head가 있어야 한다.
- 화살촉 반대편 tail이 실제 CV bus까지 연속된 픽셀 경로로 이어져야 한다.
- 숫자처럼 내부 구멍이 있는 작은 후보는 탈락시킨다.
- 일반 부하는 화살촉 이후 선로가 계속되면 안 된다.
- 지나치게 긴 경로를 따라 버스에 도달한 후보는 숫자/흐름 표식으로 본다.

구원:

- 외곽선 화살표는 YOLO가 영역을 제안할 수 있지만 CV tail-to-bus 추적을
  반드시 통과해야 한다.
- 저해상도 tiny load는 bus 가까이에 있고 triangle score와 compactness가
  충분할 때만 구원한다.
- 선로 위 방향 표식처럼 CV가 긴 관통 선로를 확인한 후보는 YOLO가
  load라고 해도 구원하지 않는다.

### 5.3 Generator

필수조건:

- YOLO 의미 confidence가 기본 기준 이상이어야 한다.
- 원형 심볼의 terminal이 실제 bus 방향 선로에 연결되어야 한다.
- 내부/외부 원 두 개가 겹친 transformer pair이면 generator에서 제외한다.

예외:

- confidence가 매우 높은 독립 `G` 범례는 객체로 표시할 수 있지만,
  연결이 없으면 `review` 상태이며 조류계산 graph에는 바로 넣지 않는다.

### 5.4 Transformer

필수조건:

- 서로 마주 보는 두 wave row 또는 겹친 두 circle winding 중 하나가 있어야
  한다.
- 두 곡선 사이 간격, 크기 유사도, 외부 lead support를 확인한다.
- 큰 범례 그림이나 단일 발전기 원은 제외한다.
- 현재 생산 정책에서는 CV pair와 YOLO 의미 확인을 함께 사용한다.

구원:

- JPEG/저해상도로 일부 곡선이 끊긴 경우에만 YOLO 영역 안에서 local Hough
  pair 검사를 한 번 수행한다.
- 원 하나 또는 파도 한 줄만 보이는 후보는 자동 구원하지 않는다.

## 6. 선로와 포트 규칙

- Bus: 막대 경계의 실제 입출입점만 포트로 사용한다.
- Load: tail/shaft 방향 포트 하나만 허용한다.
- Generator: 원형 외곽에서 bus 쪽으로 나가는 terminal 하나만 허용한다.
- Transformer: 위/아래 또는 좌/우 포트를 독립적으로 유지하며 1개만 보이는
  경우도 허용한다.
- endpoint 거리뿐 아니라 skeleton tangent와 포트 outward direction을 같이
  검사한다.
- 숫자 제거 후 생긴 작은 고립 skeleton은 선로로 사용하지 않는다.
- 하나의 load/generator에는 최종 연결을 정확히 하나만 남긴다.
- transformer는 방향이 다른 두 포트를 유지할 수 있다.
- 동일 객체 쌍 중복 경로는 load/generator에서는 제거하지만, transformer의
  서로 다른 실제 포트는 보존한다.

## 7. 그래프 전역 검증

객체별 박스가 맞더라도 다음 상태이면 결과를 `needs_review`로 표시한다.

- load 또는 generator의 연결 차수가 0이거나 2 이상
- transformer 연결 차수가 0 또는 3 이상
- 연결이 하나도 없는 bus
- 자기 자신으로 돌아오는 선로
- 존재하지 않는 객체 ID를 참조하는 선로
- 동일 포트/객체 쌍의 중복 선로
- load와 generator가 bus 없이 직접 연결된 경우

이 검사는 객체 confidence보다 우선한다. 자동 국소 재추적은 연결이 없는
객체 주변에서 한 번만 실행하고, 다시 실패하면 앱 에디터로 보낸다.

## 8. 조류계산 진입 조건

다음 조건을 모두 만족할 때만 앱의 조류계산 버튼을 활성화한다.

- 그래프 error가 없음
- 하나 이상의 bus가 존재
- 각 load/generator가 정확히 한 bus에 귀속
- transformer 양단 bus 또는 단일 표시 포트가 사용자에게 확인됨
- 섬 계통 여부가 확인됨
- Slack bus가 정확히 하나 지정됨
- line의 `rPu`, `xPu`와 load/generator 전기값이 입력됨

인식 결과만으로 전기 파라미터를 추정하지 않는다. OCR 또는 엑셀 매핑이
불확실하면 사용자가 앱에서 값을 확인한 뒤 계산한다.

## 9. 앞으로 발생할 문제와 처리 원칙

| 문제 | 자동 처리 | 실패 시 |
| --- | --- | --- |
| 저해상도/가는 선 | 좌표 보존 확대 + 국소 gap repair | review |
| 숫자가 선로에 닿음 | 객체/선로 레이어 분리 + text component 제거 | local mask 수정 |
| 흐름 화살표를 load로 인식 | tip continuation + long through-line 검사 | reject |
| 변압기를 generator로 인식 | circle-pair veto | transformer local retry |
| 교차점이 연결인지 비연결인지 불명 | 연결점 dot/bridge 분류 필요 | 사용자 확인 |
| 완전히 새로운 심볼 | 기존 조건을 억지로 완화하지 않음 | 새 symbol profile 추가 |
| 선로 일부가 객체 mask에 지워짐 | 검증된 device-to-bus attachment 보존 | local port retrace |
| 사진 회전/원근 왜곡 | orientation/deskew 전처리 추가 가능 | 지원 범위 안내 |

교차점과 완전히 새로운 심볼은 조건문만으로 안전하게 해결되지 않는다.
이 둘은 별도 분류기 또는 사용자 확인 대상으로 남기는 것이 오인식보다
안전하다.

## 10. 구현 위치

- `backend_api/core/pipeline_policy.py`
  - 이미지 프로파일
  - 적응형 threshold
  - 객체별 accept/rescue/review/reject 정책
  - 그래프 전역 검증
- `backend_api/core/adaptive_vision_pipeline.py`
  - 저/고해상도 좌표 보존 정규화
  - 기존 검출기 실행
  - 원본 좌표 복원
  - 앱용 `pipeline` 진단 보고서 생성
- `backend_api/core/vision_logic.py`
  - 실제 CV/YOLO 후보 검출과 포트 기반 선로 추적
- `backend_api/main_server.py`
  - `/analyze_image`에서 적응형 wrapper 호출
- `backend_api/tests/test_pipeline_policy.py`
  - 필수조건과 구원 불변조건 테스트

## 11. 새 규칙을 추가하는 기준

1. 서로 다른 이미지 3장 이상에서 같은 원인이 반복되는지 확인한다.
2. 좌표나 bus 번호가 아니라 구조적 증거로 규칙을 표현한다.
3. 기존 회귀 세트를 전부 실행한다.
4. 독립 holdout 이미지에서 개선되는지 확인한다.
5. precision을 떨어뜨리는 구원 규칙은 제거하거나 `review`로 낮춘다.

이 절차를 지키면 코드가 이미지 정답을 외우는 조건문 집합으로 변하는 것을
막을 수 있다.

