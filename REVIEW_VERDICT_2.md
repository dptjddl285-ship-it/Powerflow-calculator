# [REVIEW VERDICT]

## 1. 라벨 무결성 진단 및 제안된 트리거 평가
제안된 자동 오버레이 트리거(TP=0, 타겟과 예측이 각각 2개 이상)는 라벨 손상을 의심해 볼 수 있는 유용한 휴리스틱이나, 모델이 새로운 도메인에서 전혀 예측을 못하는 경우에도 오탐(False Positive)을 발생시킬 수 있습니다. 보다 강건하고 일반화된 진단 방법으로는, 정답 바운딩 박스(Label BBox)들의 면적 비율이나 종횡비가 정상 범위를 크게 벗어나는지 검사하고, 라벨 좌표가 이미지 해상도 경계를 초과하는 등 물리적 모순이 있는지를 사전에 검증하는 '구조적 이상 탐지(Structural Anomaly Detection)' 알고리즘을 도입하는 것을 권장합니다.

## 2. 하드코딩 없는 정상 이미지 처리 전략
나머지 정상 이미지에 대해 특정 파일명이나 라벨 규칙을 하드코딩하는 방식은 피해야 합니다. 대신, 모델 예측 결과에서 신뢰도(Confidence)는 높으나 정답 라벨과의 IoU가 현저히 낮은 '강한 불일치 샘플(Hard Negatives/False Positives)'의 점수를 동적으로 계산하여 정렬한 뒤, 오차 최상위 샘플들만 우선적으로 시각화하여 검수하는 Active Learning 기반의 점진적 개선 전략을 채택해야 합니다.

## 3. 데이터 누수(Leakage) 위험
손상된 데이터를 제거하는 과정에서 현재의 훈련 지표(Train 92.19%)를 의사결정의 기준으로 삼는 것은 매우 위험합니다. 훈련 세트 내부의 오류 패턴을 기반으로 필터링 규칙이나 평가 지표를 튜닝하게 되면, 전처리 파이프라인 자체가 훈련 데이터에 과적합(Overfitting)되어 실제 성능을 왜곡하는 심각한 데이터 누수(Data Leakage)를 초래합니다. 모든 검증과 룰의 유효성 평가는 반드시 훈련에 사용되지 않은 독립적인 Validation/Test 세트에서 이루어져야 합니다.
